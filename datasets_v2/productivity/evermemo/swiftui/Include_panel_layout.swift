import SwiftUI

struct Include_panel_layout: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 0) {
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
  }
}

struct Include_panel_layout_Previews: PreviewProvider {
  static var previews: some View {
    Include_panel_layout()
  }
}
