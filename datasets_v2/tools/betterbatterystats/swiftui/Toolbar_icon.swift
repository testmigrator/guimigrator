import SwiftUI

struct Toolbar_icon: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 0) {
    }
    .background(Color.clear)
    .frame(maxWidth: .infinity)
  }
}

struct Toolbar_icon_Previews: PreviewProvider {
  static var previews: some View {
    Toolbar_icon()
  }
}
