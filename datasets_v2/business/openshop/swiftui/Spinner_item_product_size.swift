import SwiftUI

struct Spinner_item_product_size: View {
  var body: some View {
    Text("").multilineTextAlignment(.center).frame(maxWidth: .infinity, alignment: .center).padding(.leading, 5.0).padding(.trailing, 1.0).padding(.top, 12.0).padding(.bottom, 12.0)
  }
}

struct Spinner_item_product_size_Previews: PreviewProvider {
  static var previews: some View {
    Spinner_item_product_size()
  }
}
