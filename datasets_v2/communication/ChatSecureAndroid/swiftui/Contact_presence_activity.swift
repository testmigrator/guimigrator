import SwiftUI

struct Contact_presence_activity: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 0) {
      Text("")
      Text("")
      Text("Security Fingerprint")
      Text("").padding(6.0)
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
    .padding(10.0)
  }
}

struct Contact_presence_activity_Previews: PreviewProvider {
  static var previews: some View {
    Contact_presence_activity()
  }
}
