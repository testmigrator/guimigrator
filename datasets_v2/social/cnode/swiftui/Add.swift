import SwiftUI

struct Add: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 0) {
      VStack(alignment: .leading, spacing: 0) {
      }
    }
  }
}

struct Add_Previews: PreviewProvider {
  static var previews: some View {
    Add()
  }
}
