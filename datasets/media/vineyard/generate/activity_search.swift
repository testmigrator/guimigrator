import SwiftUI
struct activity_search: View {

var body: some View {
Rectangle()
.frame(minWidth:0,maxWidth:.infinity,minHeight:0,maxHeight:.infinity)}}

struct activity_search_Previews: PreviewProvider {
    static var previews: some View {
        activity_search()
    }
}
