import SwiftUI
struct article_activity: View {

var body: some View {
VStack
{
HStack
{
WebView
}
.frame(minWidth:0,maxWidth:.infinity,minHeight:0,maxHeight:.infinity)
}
.frame(minWidth:0,maxWidth:.infinity,minHeight:0,maxHeight:.infinity)}}

struct article_activity_Previews: PreviewProvider {
    static var previews: some View {
        article_activity()
    }
}
