import SwiftUI
struct list_item_order_product_image: View {

var body: some View {
ZStack()
{
VStack
{
ZStack()
{}
.frame(minWidth:0,maxWidth:.infinity)
}
.frame(minWidth:0,maxWidth:.infinity)
}
.frame(minWidth:0,maxWidth:.infinity)
.padding(3)}}

struct list_item_order_product_image_Previews: PreviewProvider {
    static var previews: some View {
        list_item_order_product_image()
    }
}
