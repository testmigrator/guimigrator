import SwiftUI

struct Activity_edit_host: View {
  var body: some View {
    ZStack {
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
  }
}

struct Activity_edit_host_Previews: PreviewProvider {
  static var previews: some View {
    Activity_edit_host()
  }
}
