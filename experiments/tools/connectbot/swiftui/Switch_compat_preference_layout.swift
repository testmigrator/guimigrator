import SwiftUI

struct Switch_compat_preference_layout: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 0) {
    }
    .background(Color.clear)
  }
}

struct Switch_compat_preference_layout_Previews: PreviewProvider {
  static var previews: some View {
    Switch_compat_preference_layout()
  }
}
