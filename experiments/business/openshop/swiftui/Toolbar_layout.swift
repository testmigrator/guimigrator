import SwiftUI

struct Toolbar_layout: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 0) {
    }
    .background(Color.clear)
    .frame(maxWidth: .infinity)
  }
}

struct Toolbar_layout_Previews: PreviewProvider {
  static var previews: some View {
    Toolbar_layout()
  }
}
