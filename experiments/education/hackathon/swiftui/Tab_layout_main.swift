import SwiftUI

struct Tab_layout_main: View {
  var body: some View {
    Text("").font(.system(size: 12)).multilineTextAlignment(.center).background(Color.clear).padding(4.0)
  }
}

struct Tab_layout_main_Previews: PreviewProvider {
  static var previews: some View {
    Tab_layout_main()
  }
}
