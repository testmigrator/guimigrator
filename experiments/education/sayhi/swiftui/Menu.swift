import SwiftUI

struct Menu: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 0) {
      VStack(alignment: .leading, spacing: 0) {
      }
    }
  }
}

struct Menu_Previews: PreviewProvider {
  static var previews: some View {
    Menu()
  }
}
