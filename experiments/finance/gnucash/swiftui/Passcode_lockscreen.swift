import SwiftUI

struct Passcode_lockscreen: View {
  var body: some View {
    VStack(alignment: .leading, spacing: 0) {
      VStack(alignment: .leading, spacing: 0) {
      }
      .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
  }
}

struct Passcode_lockscreen_Previews: PreviewProvider {
  static var previews: some View {
    Passcode_lockscreen()
  }
}
