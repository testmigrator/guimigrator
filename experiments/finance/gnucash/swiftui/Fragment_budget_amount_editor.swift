import SwiftUI

struct Fragment_budget_amount_editor: View {
  var body: some View {
    ZStack {
      ScrollView {
        VStack(alignment: .leading, spacing: 0) {
        }
        .frame(maxWidth: .infinity)
      }
      .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
  }
}

struct Fragment_budget_amount_editor_Previews: PreviewProvider {
  static var previews: some View {
    Fragment_budget_amount_editor()
  }
}
